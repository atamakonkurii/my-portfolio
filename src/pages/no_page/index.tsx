import type { FC } from "react";
import { NoPageContainer } from "src/component/templates/NoPageContainer";

export const NoPage: FC = () => {
  return (
    <div>
      <NoPageContainer />
    </div>
  );
};
